import { BlockFrostAPI } from '../../../index';
export declare function health(this: BlockFrostAPI): Promise<{
    is_healthy: boolean;
}>;
export declare function healthClock(this: BlockFrostAPI): Promise<{
    server_time: number;
}>;
